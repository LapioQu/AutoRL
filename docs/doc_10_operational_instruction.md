# DOC-10 Operational Instruction

The full operational manual is stored in:

- [operations_manual.md](/E:/dipproj/docs/operations_manual.md)

Short route:

1. Create a Python `3.11` environment.
2. Install the package in editable mode.
3. Verify `python -m autorl`.
4. Use CLI, API, or UI depending on the workflow.
5. Review persisted artifacts in `artifacts/`.

This file exists as the formal `DOC-10` deliverable, while the complete operating instruction is kept in `operations_manual.md`.
